Ticket: KAN-24

Changes:
- Added root `package.json` with npm scripts for developer ergonomics (start/stop system, migrate databases).
- Installed `concurrently` to run both Node and Go microservices easily via `npm run dev:all`.
- Updated `README.md` to document the new NPM commands.
- Updated pathing in `README.md` to reflect the latest `infra/docker-compose.yml` and `services/` directory structure.

How did we test these changes:
- Executed `npm run docker:up` successfully.
- Verified path changes align with the current repo directory layout.
